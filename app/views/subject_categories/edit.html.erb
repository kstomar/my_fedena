<div id="content-header">
  <%= image_tag("/images/show_settings.png") %>
  <h1><%= t('subject') %></h1>
  <h3><%= t('categories') %></h3>
  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png",:border => 0), "history.back()" %>
  </div>
</div>

<div id="page-yield">
  <div id="flash_box"></div>
  <% unless flash[:notice].nil? %>
    <p class="flash-msg"> <%= flash[:notice] %> </p>
  <% end %>
  <div id="student-category-form">
    <div class="box">
			<% form_for @subject_category do |f| %>

			<div class="label-field-pair">
			  <label><%= t('edit_category') %>: <%= @subject_category.name %> </label>
			  <%= hidden_field_tag :id, @subject_category.id %>
			<div class="text-input-bg"> <%= text_field_tag :name, @subject_category.name %></div></div>
			  <%= submit_tag "", :value => "► #{t('save')}", :class => "submit_button" %>
			<% end %>

		</div>


  </div>

 
</div>
